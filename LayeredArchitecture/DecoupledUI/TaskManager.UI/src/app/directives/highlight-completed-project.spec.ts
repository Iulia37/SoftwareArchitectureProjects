import { HighlightCompletedProject } from './highlight-completed-project';

describe('HighlightCompletedProject', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedProject();
    expect(directive).toBeTruthy();
  });
});
